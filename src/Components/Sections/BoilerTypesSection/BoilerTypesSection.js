import React, { useEffect } from 'react';

const BoilerTypesSection = (props) => {
  useEffect(() => {
    props.setHeaderTitle('Boiler Types');
  });
  return (
        <h2>TO DO BOILER TYPES</h2>
  );
};

export default BoilerTypesSection;
